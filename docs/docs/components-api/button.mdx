---
id: api-button
title: Button
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

import AlertVariantsExampleSource from '!!raw-loader!../../src/examples/components/Alert/variants'
import AlertVariantsExample from '../../src/examples/components/Alert/variants'
import AlertExampleSource from '!!raw-loader!../../src/examples/components/Alert/index.tsx'
import AlertExample from '../../src/examples/components/Alert/index.tsx'

An alert displays a short, important message in a way that attracts the user's attention without interrupting the user's task.

### Variants

<AlertVariantsExample />

<CodeBlock className="language-tsx">{AlertVariantsExampleSource}</CodeBlock>

### Variants using tabs

We can separate our code using tabs! A tab can be use to show a summary code and other to show the full source of a component!

<Tabs
  className="custom-tabs"
  defaultValue="code"
  values={[
    {label: 'Code', value: 'code'},
    {label: 'Full Source', value: 'full-source'},
  ]}>
<TabItem value="code">

```jsx
import Alert from '@eduzz/houston-ui/Alert';

<Alert>Success</Alert>
<Alert type='info'>Info</Alert>
<Alert type='error'>Error</Alert>
<Alert type='warning'>Warning</Alert>
```

</TabItem>
<TabItem value="full-source">
  <CodeBlock className="language-tsx">{AlertVariantsExampleSource}</CodeBlock>
</TabItem>
</Tabs>

### Examples

<AlertExample />

<CodeBlock className="language-tsx">{AlertExampleSource}</CodeBlock>

### Props available

The props avaible of the Alert component. Learn more about the props:

| prop | type | required | default | description |
| ---- | ---- | -------- | ------- | ----------- |
| type | `success`, `info`, `warning`, `error` | false | `success` | - |
| onClose | `(event: React.SyntheticEvent) => void` | false | - | If there is a value, it automatically adds the "X". |
| icon | `React.ReactNode`, `false` | false | `type` icon | If `false`, the icon will be hidden. |
| title | `React.ReactNode` | false | - | - |
| closable | `boolean` | false | false | If true, the component controls the state itself and there is no need to use the `onClose` prop. |
| actions | `IActions[]` | false | [] | - |

### API

- [```<Alert />```](https://google.com)



