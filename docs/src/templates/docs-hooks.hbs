---
id: {{dashCase id}}
title: {{title}}
---

<!-- 
  *** IMPORTANTE:
  *** Para que sua documentação apareça na sidebar, adicione seu caminho através do arquivo sidebars.js
-->

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

<!-- Adicione aqui um texto descrevendo sobre o hook -->
Descreva aqui um pouco sobre o hook do Houston, qual sua finalidade, etc.

<!-- 
  *** Seções destinadas a apresentar o hook com exemplos, variações, demonstração de como funciona algumas propriedades, etc.
  *** Crie quantas seções forem necessárias para documentar o hook
-->
### Seção 1

Lorem ipsum dolor, sit amet consectetur adipisicing elit. Vero debitis eum, esse id iusto reiciendis quasi aliquid rerum, illo velit nisi sapiente soluta ratione est? Sit provident aperiam exercitationem incidunt!

<!-- 
  *** Bloco de código com o exemplo do hook que está sendo documentado
  *** Mantenha as configurações do componente Tabs como está
  *** Altere apenas os conteúdos dos códigos-fonte
  *** Caso queira demonstrar utilizando um arquivo externo, importe o arquivo de código-fonte utilizando !!raw-loader (https://v2.docusaurus.io/examples/markdownPageExample)
-->
<Tabs
  className="custom-tabs"
  defaultValue="code"
  values={[
    {label: 'Código-fonte', value: 'code'},
    {label: 'Código-fonte completo', value: 'full-source'},
  ]}>
<TabItem value="code">
{/* Insira o código resumido de como usar o hook, incluindo a importação */}

```jsx
import useObservable from '@eduzz/houston-hooks/useObservable';

useObservable(() => {
  return submitData.pipe(
    switchMap(({ model, formikHelpers }) => {
      const result$ = onSubmit && onSubmit(model as Values, formikHelpers);

      const result = of(true).pipe(
        switchMap(() => (!result$ ? of(null) : result$)),
        catchError(() => of(null)),
        share()
      );

      promiseRef.promise = result.toPromise();

      return result;
    })
  );
}, []);
```

</TabItem>
<TabItem value="full-source">
  {/* Referencie o código do componente que criou para demonstrar a utilização do hook do Houston usando !!raw-loader! */}

  <CodeBlock className="language-tsx">
    Adicione o código completo de um componente usando o hook aqui
  </CodeBlock>
</TabItem>
</Tabs>

### Seção 2

Lorem ipsum dolor sit amet consectetur adipisicing elit. Voluptatem, ipsum. Doloribus, ipsam id? Ratione blanditiis quod at repudiandae esse cupiditate.

:::tip Caixa de aviso

Caso queira incluir avisos, você pode usar essas caixas contendo os valores: `info`, `note`, `tip`, `caution` ou `danger`.

:::
