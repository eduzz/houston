---
name: Tokens
---

import { Playground } from 'dokz';

import createTokens from '.';

# Tokens

Valores padrão.

<code className='language-json'>{JSON.stringify(createTokens('orbita'), null, 2)}</code>
