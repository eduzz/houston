import Content from './Content';
import Layout from './Layout';
import Sidebar, { ISidebarProps as SidebarProps } from './Sidebar';

export { Sidebar, Content };
export type ISidebarProps = SidebarProps;

export default Layout;
