---
name: Input
---

import InsertEmoticonIcon from '@mui/icons-material/InsertEmoticon';
import { Playground } from 'dokz';

import cnpjMask from '@eduzz/houston-forms/masks/cnpj';
import cpfMask from '@eduzz/houston-forms/masks/cpf';
import documentMask from '@eduzz/houston-forms/masks/document';
import moneyMask from '@eduzz/houston-forms/masks/money';
import phoneMask from '@eduzz/houston-forms/masks/phone';
import zipcodeMask from '@eduzz/houston-forms/masks/zipcode';

import Input from './';

# Input

O componente é responsável por exibir um campo de texto para ser preenchido em formulários, campos de pesquisa, entre outros.

### Importação

```js
import Input from '@eduzz/houston-ui/Forms/Input';
```

### Exemplos

<Playground>
  {() => {
    const [text, setText] = React.useState('');
    return (
      <div style={{ display: 'flex', flexDirection: 'column', flexWrap: 'wrap', gap: 16 }}>
        <Input label='Campo de Texto' value={text} onChange={setText} />
        <Input label='Campo com ícone' value={text} startAdornment={<InsertEmoticonIcon />} onChange={setText} />
        <Input label='Campo com ícone' value={text} endAdornment={<InsertEmoticonIcon />} onChange={setText} />
        <Input label='Campo de Texto' placeholder='Placeholder' value={text} onChange={setText} />
        <Input label='Campo de Texto Desabilitado' disabled value='Valor enviado via propriedade' />
        <Input label='Campo de Texto com Carregamento' loading value={text} onChange={setText} />
        <Input
          label='Campo de Texto com Texto de Ajuda'
          helperText='Um exemplo de campo de ajuda'
          value={text}
          onChange={setText}
        />
        <Input label='Campo de Texto com Erro' errorMessage='Mensagem de erro' value={text} onChange={setText} />
      </div>
    );
  }}
</Playground>

### Componente de texto com múltiplas linhas

<Playground>
  {() => {
    const [text, setText] = React.useState('');
    return (
      <div style={{ display: 'flex', flexDirection: 'column', flexWrap: 'wrap', gap: 16 }}>
        <Input multiline label='Campo de Texto' value={text} onChange={setText} />
        <Input
          multiline
          label='Campo com ícone'
          value={text}
          startAdornment={<InsertEmoticonIcon />}
          onChange={setText}
        />
        <Input
          multiline
          label='Campo com ícone'
          value={text}
          endAdornment={<InsertEmoticonIcon />}
          onChange={setText}
        />
        <Input multiline label='Campo de Texto' placeholder='Placeholder' value={text} onChange={setText} />
        <Input multiline label='Campo de Texto Desabilitado' disabled value='Valor enviado via propriedade' />
        <Input multiline label='Campo de Texto com Carregamento' loading value={text} onChange={setText} />
        <Input
          multiline
          label='Campo de Texto com Texto de Ajuda'
          helperText='Um exemplo de campo de ajuda'
          value={text}
          onChange={setText}
        />
        <Input
          multiline
          label='Campo de Texto com Erro'
          errorMessage='Mensagem de erro'
          value={text}
          onChange={setText}
        />
      </div>
    );
  }}
</Playground>

### Máscaras

```js
import maskPhone from '@eduzz/houston-forms/masks/phone';
```

<Playground>
  {() => {
    const [text, setText] = React.useState('');
    const [money, setMoney] = React.useState();
    const [phone, setPhone] = React.useState('');
    const [zipcode, setZipcode] = React.useState('');
    const [document, setDocument] = React.useState('');
    const [cpf, setCpf] = React.useState('');
    const [cnpj, setCnpj] = React.useState('');
    return (
      <div style={{ display: 'flex', flexDirection: 'column', flexWrap: 'wrap', gap: 16 }}>
        <Input label='Moeda' name='money' value={money} onChange={setMoney} mask={moneyMask} />
        <Input label='Telefone' name='phone' value={phone} onChange={setPhone} mask={phoneMask} />
        <Input label='CEP' name='zipcode' value={zipcode} onChange={setZipcode} mask={zipcodeMask} />
        <Input
          label='Documento (CPF/CNPJ)'
          name='document'
          value={document}
          onChange={setDocument}
          mask={documentMask}
        />
        <Input label='CNPJ' name='cnpj' value={cnpj} onChange={setCnpj} mask={cnpjMask} />
        <Input label='CPF' name='cpf' value={cpf} onChange={setCpf} mask={cpfMask} />
      </div>
    );
  }}
</Playground>

### Props

| prop              | tipo                         | obrigatório | padrão  | descrição                                                                           |
| ----------------- | ---------------------------- | ----------- | ------- | ----------------------------------------------------------------------------------- |
| id                | `string`                     | `false`     | -       | -                                                                                   |
| name              | `string`                     | `true`      | -       | -                                                                                   |
| label             | `string`                     | `false`     | -       | -                                                                                   |
| type              | `React.InputHTMLAttributes`  | `false`     | `text`  | -                                                                                   |
| className         | `string`                     | `false`     | -       | -                                                                                   |
| placeholder       | `string`                     | `false`     | -       | -                                                                                   |
| disabled          | `boolean`                    | `false`     | `false` | -                                                                                   |
| fullWidth         | `boolean`                    | `false`     | `true`  | -                                                                                   |
| multiline         | `boolean`                    | `false`     | `false` | Permite um campo de texto com várias linhas.                                        |
| rows              | `2, 4, 6, 8, 10, 14, 18, 24` | `false`     | -       | Número de linhas quando `multiline` for verdadeiro.                                 |
| disableAutoResize | `boolean`                    | `false`     | -       | Desativa o auto-resize do multiline                                                 |
| loading           | `boolean`                    | `false`     | -       | Exibe ícone de carregamento no componente de texto.                                 |
| helperText        | `string`                     | `false`     | -       | Exibe texto de ajuda abaixo do componente de texto.                                 |
| errorMessage      | `string`                     | `false`     | -       | Exibe uma mensagem de erro no componente de texto, assim como indica erro no campo. |
| mask              | `IFormMask`                  | `false`     | -       | Máscara que será aplicada no campo.                                                 |
| value             | `any`                        | `false`     | -       | -                                                                                   |
| onChange          | `function`                   | `false`     | -       | Função disparada ao alterar o valor do campo de texto.                              |
| onPressEnter      | `function`                   | `false`     | -       | Função disparada ao apertar Enter dentro do campo de texto.                         |
| endAdornment      | `React.ReactNode`            | `false`     | -       | Ícone ou botão com ícone que será exibido no final do campo.                        |
| startAdornment    | `React.ReactNode`            | `false`     | -       | Ícone ou botão com ícone que será exibido no início do campo.                       |
