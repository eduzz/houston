---
name: StepperVertical
---

import { Playground } from 'dokz';

import Stepper from '@eduzz/houston-ui/Stepper';
import Button from '@eduzz/houston-ui/Button';
import Column from '@eduzz/houston-ui/Grid/Column';
import Row from '@eduzz/houston-ui/Grid/Row';

# StepperVertical

O Stepper tem como objetivo mostrar ao usuário a quantidade de etapas dentro de um fluxo, bem como indicar qualquer etapa que está sendo apresentada.

### Importação

```js
import Stepper from '@eduzz/houston-ui/Stepper';
const { Step } = Stepper;
```

### Valor controlado e prop noClick

<Playground>
  {() => {
    const { Step } = Stepper;
    const [current, setCurrent] = React.useState(0);
    const goToNext = () => {
      setCurrent(current + 1);
    };
    const goToPrev = () => {
      setCurrent(current - 1);
    };
    return (
      <Stepper.Vertical current={current} noClick>
        <Step label='Básico'>
          <div>Conteúdo1</div>
          <Row>
            <Column>
              <Button onClick={goToNext}>Próximo</Button>
            </Column>
          </Row>
        </Step>
        <Step label='Personalização' description='Personalize seu produto'>
          <div>Conteúdo2</div>
          <Row>
            <Column>
              <Button onClick={goToPrev} variant='outline'>
                Anterior
              </Button>
            </Column>
            <Column>
              <Button onClick={goToNext}>Próximo</Button>
            </Column>
          </Row>
        </Step>
        <Step label='Comunicação' description='Comunicação é muito importante'>
          <div>Conteúdo3</div>
          <Row>
            <Column>
              <Button onClick={goToPrev} variant='outline'>
                Anterior
              </Button>
            </Column>
            <Column>
              <Button onClick={goToNext}>Próximo</Button>
            </Column>
          </Row>
        </Step>
        <Step label='Avançado'>
          <div>Conteúdo4</div>
          <Row>
            <Column>
              <Button onClick={goToPrev} variant='outline'>
                Anterior
              </Button>
            </Column>
            <Column>
              <Button onClick={goToNext}>Finalizar</Button>
            </Column>
          </Row>
        </Step>
      </Stepper.Vertical>
    );
  }}
</Playground>

### Auto gerenciável

Não há necessidade de controlar o estado do step ativo.

<Playground>
  {() => {
    const { Step } = Stepper;
    return (
      <Stepper.Vertical>
        <Step label='Passo 1'>Conteúdo 1</Step>
        <Step label='Passo 2'>Conteúdo 2</Step>
        <Step label='Passo 3'>Conteúdo 3</Step>
        <Step label='Passo 4' description='Etapa final'>
          Conteúdo 4
        </Step>
      </Stepper.Vertical>
    );
  }}
</Playground>

### Somente como indicador(sem conteúdo) e error

<Playground>
  {() => {
    const { Step } = Stepper;
    return (
      <Stepper.Vertical>
        <Step label='Básico' />
        <Step label='Personalização' description='Personalize seu produto' />
        <Step error label='Comunicação' description='Comunicação é muito importante' />
        <Step label='Avançado' />
      </Stepper.Vertical>
    );
  }}
</Playground>

### Stepper props

| prop           | type               | required | default | description                                                                                |
| -------------- | ------------------ | -------- | ------- | ------------------------------------------------------------------------------------------ |
| onPrev         | `(number) => void` | `false`  | -       | Passa pelo callback o índice do Step                                                       |
| onNext         | `(number) => void` | `false`  | -       | Passa pelo callback o índice do Step                                                       |
| current        | `number`           | `false`  | `false` | Índice do Step atual                                                                       |
| children       | `React.ReactNode`  | `true`   | -       |                                                                                            |
| clickable      | `boolean`          | `false`  | `false` | Habilita o click no Step                                                                   |
| destroyOnClose | `boolean`          | `false`  | `false` | Se true o componente que está dentro do Step será desmontado depois que o Step for trocado |
| mountOnEnter   | `boolean`          | `false`  | `false` | Se true o componente que está dentro do Step só será montado depois que o Step for trocado |

### Step props

| prop        | type              | required | default | description |
| ----------- | ----------------- | -------- | ------- | ----------- |
| children    | `React.ReactNode` | `false`  | -       |             |
| error       | `boolean`         | `false`  | `false` |             |
| label       | `string`          | `true`   | -       |             |
| description | `string`          | `false`  | -       |             |
