---
name: Tabs
---

import InsertEmoticonIcon from '@mui/icons-material/InsertEmoticon';
import FaceHappyOutline from '@eduzz/houston-icons/FaceHappyOutline';
import { Playground } from 'dokz';
import Image from '@eduzz/houston-ui/Image';

import Tabs from './';

# Tabs

As abas facilitam a exploração e alternam entre diferentes visualizações.

### Importação

```js
import Tabs from '@eduzz/houston-ui/Tabs';
const { Tab } = Tabs;
```

### Auto gerenciável

Não há necessidade de controlar o estado da aba ativa.

<Playground>
  {() => {
    const { Tab } = Tabs;
    return (
      <Tabs>
        <Tab label='Label 1'>
          Conteúdo 1
          <Image src='https://picsum.photos/301' />
        </Tab>
        <Tab label='Label 2'>
          Conteúdo 2
          <Image src='https://picsum.photos/300' />
        </Tab>
      </Tabs>
    );
  }}
</Playground>

### Valor controlado

<Playground>
  {() => {
    const { Tab } = Tabs;
    const [tab, setTab] = React.useState(0);
    return (
      <Tabs value={tab} onChange={value => setTab(value)}>
        <Tab label='Label 1' icon={<FaceHappyOutline />}>
          <div>Conteúdo 1</div>
        </Tab>
        <Tab label='Label 2' icon={<FaceHappyOutline />}>
          <div>Conteúdo 2</div>
        </Tab>
      </Tabs>
    );
  }}
</Playground>

<!-- ### Desabilitado

<Playground>
  <Tabs>
    <Tab label='Título 1'>
      <div>Conteúdo 1</div>
    </Tab>
    <Tab label='Desabilitado 2' disabled>
      <div>Conteúdo 2</div>
    </Tab>
  </Tabs>
</Playground>

### Com ícone

<Playground>
  <Tabs>
    <Tab label='Título 1' icon={<InsertEmoticonIcon />}>
      <div>Conteúdo 1</div>
    </Tab>
    <Tab label='Título 2' icon={<InsertEmoticonIcon />}>
      <div>Conteúdo 2</div>
    </Tab>
  </Tabs>
</Playground> -->

#### Tabs props

| prop     | type                      | required | default | description         |
| -------- | ------------------------- | -------- | ------- | ------------------- |
| value    | `number`                  | `false`  | -       | Índice da aba.      |
| onChange | `(value: number) => void` | `false`  | -       | Recebe novo índice. |

#### Tab props

| prop            | type        | required | default |
| --------------- | ----------- | -------- | ------- |
| label           | `string`    | `true`   | -       |
| icon            | `ReactNode` | `false`  | -       |
| disabled        | `boolean`   | `false`  | `false` |
| disabledPadding | `boolean`   | `false`  | `false` |
