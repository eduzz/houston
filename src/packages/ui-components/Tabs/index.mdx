---
name: Tabs
route: /components/tabs
menu: Componentes
---

import InsertEmoticonIcon from '@material-ui/icons/InsertEmoticon';

import { Playground } from 'docz';

import Tabs from './';

# Tabs

As abas facilitam a exploração e alternam entre diferentes visualizações.

### Importação

```js
import Tabs from '@eduzz/houston-ui/Tabs';
```

### Auto gerenciável

Não há necessidade de controlar o estado da aba ativa.

<Playground>
  <Tabs>
    <Tabs.Content label='Título 1'>Conteúdo 1</Tabs.Content>
    <Tabs.Content label='Título 2'>Conteúdo 2</Tabs.Content>
  </Tabs>
</Playground>

### Valor controlado

<Playground>
  {() => {
    const [tab, setTab] = React.useState(0);
    return (
      <Tabs value={tab} onChange={value => setTab(value)}>
        <Tabs.Content label='Título 1'>Conteúdo 1</Tabs.Content>
        <Tabs.Content label='Título 2'>Conteúdo 2</Tabs.Content>
      </Tabs>
    );
  }}
</Playground>

### Estado

<Playground>
  <Tabs>
    <Tabs.Content label='Título 1'>Conteúdo 1</Tabs.Content>
    <Tabs.Content label='Desabilitado 2' disabled>
      Conteúdo 2
    </Tabs.Content>
  </Tabs>
</Playground>

### Com ícone

<Playground>
  <Tabs>
    <Tabs.Content label='Título 1' icon={<InsertEmoticonIcon />}>
      Conteúdo 1
    </Tabs.Content>
    <Tabs.Content label='Título 2' icon={<InsertEmoticonIcon />}>
      Conteúdo 2
    </Tabs.Content>
  </Tabs>
</Playground>

## Propriedades

### Tabs props

| prop     | type                      | required | default | description         |
| -------- | ------------------------- | -------- | ------- | ------------------- |
| value    | `number`                  | `false`  | -       | Índice da aba.      |
| onChange | `(value: number) => void` | `false`  | -       | Recebe novo índice. |

### Tabs.Content props

| prop            | type        | required | default |
| --------------- | ----------- | -------- | ------- |
| label           | `string`    | `true`   | -       |
| icon            | `ReactNode` | `false`  | -       |
| disabled        | `boolean`   | `false`  | `false` |
| disabledPadding | `boolean`   | `false`  | `false` |
