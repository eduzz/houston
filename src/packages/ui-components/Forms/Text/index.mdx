---
name: Text
route: /componentes/text
menu: eduzz/houston-ui
submenu: Componentes
---

import cnpjMask from '@eduzz/houston-forms/masks/cnpj';
import cpfMask from '@eduzz/houston-forms/masks/cpf';
import documentMask from '@eduzz/houston-forms/masks/document';
import moneyMask from '@eduzz/houston-forms/masks/money';
import phoneMask from '@eduzz/houston-forms/masks/phone';
import zipcodeMask from '@eduzz/houston-forms/masks/zipcode';

import { Playground } from 'docz';

import TextField from './';

# Text

O componente é responsável por exibir um campo de texto para ser preenchido em formulários, campos de pesquisa, entre outros.

### Importação

```js
import TextField from '@eduzz/houston-ui/Forms/Text';
```

### Exemplos

<Playground>
  {() => {
    const [text, setText] = React.useState('');
    return (
      <>
        <TextField label='Campo de Texto' value={text} onChange={setText} />
        <TextField label='Campo de Texto' placeholder='Placeholder' value={text} onChange={setText} />
        <TextField label='Campo de Texto Desabilitado' disabled value='Valor enviado via propriedade' />
        <TextField label='Campo de Texto com Carregamento' loading value={text} onChange={setText} />
        <TextField
          label='Campo de Texto com Texto de Ajuda'
          helperText='Um exemplo de campo de ajuda'
          value={text}
          onChange={setText}
        />
        <TextField label='Campo de Texto com Erro' errorMessage='Mensagem de erro' value={text} onChange={setText} />
      </>
    );
  }}
</Playground>

### Componente de texto com múltiplas linhas

<Playground>
  {() => {
    const [text, setText] = React.useState('');
    return (
      <>
        <TextField label='Texto com múltiplas linhas' value={text} onChange={setText} multiline />
        <TextField label='Texto com múltiplas linhas' disabled value={text} onChange={setText} multiline />
        <TextField
          label='Texto com múltiplas linhas com erro'
          errorMessage='Mensagem de erro'
          value={text}
          onChange={setText}
          multiline
        />
      </>
    );
  }}
</Playground>

### Máscaras

<Playground>
  {() => {
    const [text, setText] = React.useState('');
    const [money, setMoney] = React.useState();
    const [phone, setPhone] = React.useState('');
    const [zipcode, setZipcode] = React.useState('');
    const [document, setDocument] = React.useState('');
    const [cpf, setCpf] = React.useState('');
    const [cnpj, setCnpj] = React.useState('');
    return (
      <>
        <TextField label='Moeda' name='money' value={money} onChange={setMoney} mask={moneyMask} />
        <TextField label='Telefone' name='phone' value={phone} onChange={setPhone} mask={phoneMask} />
        <TextField label='CEP' name='zipcode' value={zipcode} onChange={setZipcode} mask={zipcodeMask} />
        <TextField label='Documento' name='document' value={document} onChange={setDocument} mask={documentMask} />
        <TextField label='CNPJ' name='cnpj' value={cnpj} onChange={setCnpj} mask={cnpjMask} />
        <TextField label='CPF' name='cpf' value={cpf} onChange={setCpf} mask={cpfMask} />
      </>
    );
  }}
</Playground>

### Propriedades

| prop         | tipo                        | obrigatório | padrão  | descrição                                                                          |
| ------------ | --------------------------- | ----------- | ------- | ---------------------------------------------------------------------------------- |
| id           | `string`                    | `false`     | -       |                                                                                    |
| name         | `string`                    | `true`      | -       |                                                                                    |
| label        | `React.ReactNode`           | `false`     | -       |                                                                                    |
| type         | `React.InputHTMLAttributes` | `false`     | `text`  |                                                                                    |
| placeholder  | `string`                    | `false`     | -       |                                                                                    |
| disabled     | `boolean`                   | `false`     | `false` |                                                                                    |
| fullWidth    | `boolean`                   | `false`     | `false` |                                                                                    |
| multiline    | `boolean`                   | `false`     | `false` | Permite um campo de texto com várias linhas                                        |
| className    | `string`                    | `false`     | -       |                                                                                    |
| loading      | `boolean`                   | `false`     | -       | Exibe ícone de carregamento no componente de texto                                 |
| helperText   | `string`                    | `false`     | -       | Exibe texto de ajuda abaixo do componente de texto                                 |
| errorMessage | `string`                    | `false`     | -       | Exibe uma mensagem de erro no componente de texto, assim como indica erro no campo |
| form         | `IFormAdapter`              | `false`     | -       |                                                                                    |
| mask         | `IFormMask`                 | `false`     | -       | Máscara que será aplicada no campo                                                 |
| value        | `any`                       | `false`     | -       |                                                                                    |
| margin       | `none`, `dense`, `normal`   | `normal`    | -       |                                                                                    |
| onChange     | `function`                  | `false`     | -       | Função disparada ao alterar o valor do campo de texto                              |
| onBlur       | `function`                  | `false`     | -       | Função disparada ao tirar o foco do componente                                     |
